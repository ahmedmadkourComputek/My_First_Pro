```text
# Simple Website

This repository contains a minimal website root.

Files:
- index.html — the homepage
- styles.css — minimal styles

How to preview locally (in Codespace or locally):
- Python 3: `python3 -m http.server 8000` then open the forwarded port / http://localhost:8000
```